<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rose" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".welcome.Welcome"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".searchDisease.SearchDisease"
            android:label="IDoctor"
            />

       <activity
            android:name=".searchhospital.SearchHospital"
            android:label="IDoctor"
        />

        <activity
            android:name=".bmi.BMIcaculator"
            android:label="IDoctor"
        />

        <activity
            android:name=".game.Game"
            android:label="IDoctor"
        />

    </application>

</manifest>
